USE ecommercedb;

SELECT
	u.first_name
,	o.order_status
FROM orders o
JOIN users u ON o.user_id = u.user_id
WHERE order_status = 'shipped';

SELECT
	u.last_name,
FROM users
WHERE last_name = 'Smith';

